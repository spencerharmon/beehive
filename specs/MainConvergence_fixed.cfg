\* Fixed protocol, pre-receive guard on: all safety invariants hold and the
\* system always converges. This is the idealized beehive convergence protocol.
SPECIFICATION Spec
CONSTANTS
    Artifacts = {a1, a2, a3}
    Fixed = TRUE
    PreReceiveGuard = TRUE
    StagedAtomic = TRUE
INVARIANTS
    TypeOK
    Reconcilable
    NoSilentLoss
    NoStagedLoss
PROPERTIES
    EventuallyConverged
