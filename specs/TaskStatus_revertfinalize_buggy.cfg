\* THE REVIEW-REVERT CONTENT-FINALIZE false-DONE (live flux:omada-ingress-tls,
\* 2026-08-02): the interrupted-review finalize shortcut (FinalizeAlreadyMerged)
\* concluded "already merged into tracked main ... runner-finalized DONE (no
\* re-review, branch not required)" from commit ANCESTRY -- the merge commit
\* (6f34902) stays reachable in `main`'s history -- NOT from the reviewed TREE
\* CONTENT surviving. A revert-after-merge (7f36d00 `Revert "Merge branch
\* bee-omada-ingress-tls"`, 14 min later) removed all five delivered files while
\* the merge stayed an ancestor, so ancestry-only finalize flipped DONE with the
\* ingress ABSENT from `main` and the target's own Check never run.
\* ContentFinalize = FALSE (ancestry-only finalize -- the bug). Every OTHER DONE
\* gate stays ON (Gated, CheckGated, RevertOverPin all TRUE), isolating this
\* Review-path content hole from the older durability/DoD/leak defects: TLC
\* reaches a DONE state with contentPresent = FALSE, violating NoFalseDone with a
\* revert-after-merge counterexample trace. Same corpus-eating shape as the
\* empty-checksum canonical bug, one layer down on the Review finalize path.
SPECIFICATION Spec
CONSTANTS
    Limit = 1
    Gated = TRUE
    CheckGated = TRUE
    RevertOverPin = TRUE
    ContentFinalize = FALSE
    ModelResolve = FALSE
    ResetAttemptsOnResolve = TRUE
INVARIANTS
    TypeOK
    NoFalseDone
