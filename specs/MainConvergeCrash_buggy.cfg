\* CURRENT code: both defenses OFF (PushFirst=FALSE commits local before push;
\* HealOnFork=FALSE = the ff-only pullMain that never merges a fork). This is the
\* path in force on 2026-09-16 when the pillar hive forked. TLC finds the exact
\* trace: CommitLocal(a1) -> local={a1} ahead; ExternalPush(a2) -> {a1} vs {a2}
\* FORK. LocalNeverAhead and Reconcilable are violated.
SPECIFICATION Spec
CONSTANTS
    Artifacts = {a1, a2, a3}
    PushFirst = FALSE
    HealOnFork = FALSE
INVARIANTS
    TypeOK
    LocalNeverAhead
    Reconcilable
    NoSilentLoss
