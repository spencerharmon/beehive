\* b08c995 (namespace-capture half): Manager claims the bare edit-* namespace and
\* reclaims a FOREIGN subsystem's worktree once it looks clean. NoForeignReclaim
\* violated, with trace.
SPECIFICATION Spec
CONSTANTS
    NamespaceScoped = FALSE
    LiveGuard = TRUE
    RemoteDurable = TRUE
INVARIANTS
    TypeOK
    NoForeignReclaim
    LiveSessionNeverReclaimed
    SessionDurable
