\* Buggy direct-on-primary path (no SyncMainFromRemote before author).
\* Reproduces the b48b927 fork: TLC finds a state where mainLocal and
\* mainRemote are incomparable -> Reconcilable is violated, with the trace.
SPECIFICATION Spec
CONSTANTS
    Artifacts = {a1, a2, a3}
    Fixed = FALSE
    PreReceiveGuard = TRUE
    StagedAtomic = TRUE
    ConflictModel = FALSE
    ConflictHonest = TRUE
    MaxResolveAttempts = 3
INVARIANTS
    TypeOK
    Reconcilable
