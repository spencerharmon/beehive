\* THE ALREADY-MERGED REVIEW SHORTCUT FOOLED BY A REVERT (flux:omada-ingress-tls,
\* 2026-08-02): the review "already-merged" finalize (FinalizeAlreadyMerged /
\* swarm.go finalizeIfAlreadyMerged) concludes a task is DONE from bare commit
\* ANCESTRY -- the merge commit stays in `main`'s history -- NOT from the reviewed
\* TREE CONTENT surviving. A `Revert "Merge branch bee-omada-ingress-tls"` landed
\* 14 min after the merge, deleting all five delivered files; the merge commit
\* remained an ancestor, so the ancestry-only shortcut finalized DONE with the
\* ingress absent from the cluster and the task's Check never run. ModelRevert
\* enables the interrupted-merge + revert-after-merge trace; ContentGated = FALSE is
\* the buggy ancestry-only finalize. Durability and DoD-check gates BOTH ON
\* (isolating the content-vs-ancestry defect from the older false-DONE families):
\* a DONE state is still reachable with the reviewed artifact ABSENT from `main`
\* (artifactOnMain = FALSE). NoFalseDone violated, with counterexample trace.
SPECIFICATION Spec
CONSTANTS
    Limit = 1
    Gated = TRUE
    CheckGated = TRUE
    RevertOverPin = TRUE
    ModelResolve = FALSE
    ResetAttemptsOnResolve = TRUE
    ModelRevert = TRUE
    ContentGated = FALSE
INVARIANTS
    TypeOK
    NoFalseDone
