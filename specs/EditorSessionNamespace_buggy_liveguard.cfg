\* b08c995 (live-wipe half): the gc dance reclaims an operator's open-but-idle,
\* stale-record LIVE session. LiveSessionNeverReclaimed violated, with trace.
SPECIFICATION Spec
CONSTANTS
    NamespaceScoped = TRUE
    LiveGuard = FALSE
    RemoteDurable = TRUE
INVARIANTS
    TypeOK
    NoForeignReclaim
    LiveSessionNeverReclaimed
    SessionDurable
