\* All three protections on: no foreign worktree touched, no live session
\* reclaimed, no pending edit ever lost. No error.
SPECIFICATION Spec
CONSTANTS
    NamespaceScoped = TRUE
    LiveGuard = TRUE
    RemoteDurable = TRUE
INVARIANTS
    TypeOK
    NoForeignReclaim
    LiveSessionNeverReclaimed
    SessionDurable
