\* Fixed authoring, but pre-receive guard OFF: an external non-ff force-rewind
\* of main drops an already-committed artifact. Reproduces the f8e7828 gap:
\* NoSilentLoss is violated, with the trace.
SPECIFICATION Spec
CONSTANTS
    Artifacts = {a1, a2, a3}
    Fixed = TRUE
    PreReceiveGuard = FALSE
INVARIANTS
    TypeOK
    NoSilentLoss
