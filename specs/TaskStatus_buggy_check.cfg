\* No definition-of-done gate (pre-92d2ed1): a task is approved to DONE on real,
\* durable, merged work while its declared acceptance Check is NOT satisfied --
\* the jellyfin:zuul-image-build-publish false-DONE (config commit reviewed and
\* merged, but the image was never pullable). Durability gate ON, isolating the
\* DoD defect: NoFalseDone violated, with trace.
SPECIFICATION Spec
CONSTANTS
    Limit = 1
    Gated = TRUE
    CheckGated = FALSE
INVARIANTS
    TypeOK
    NoFalseDone
