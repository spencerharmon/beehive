\* c64efe7 pre-fix: no trusted-remote push, so a pending session that loses its
\* local worktree is unrecoverable. SessionDurable violated, with trace.
SPECIFICATION Spec
CONSTANTS
    NamespaceScoped = TRUE
    LiveGuard = TRUE
    RemoteDurable = FALSE
INVARIANTS
    TypeOK
    NoForeignReclaim
    LiveSessionNeverReclaimed
    SessionDurable
