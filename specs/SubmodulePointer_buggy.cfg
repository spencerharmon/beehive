\* Old protocol: the agent bumps the gitlink to its bee tip. After the branch is
\* merged (new tracked tip) and reclaimed (bee tip GC'd off origin), the gitlink
\* names an unresolvable sha. Reproduces the 1a9bcea/35442f4 dangling pointer:
\* PointerDurable is violated, with the trace.
SPECIFICATION Spec
CONSTANTS
    MaxTasks = 2
    Fixed = FALSE
INVARIANTS
    TypeOK
    PointerDurable
