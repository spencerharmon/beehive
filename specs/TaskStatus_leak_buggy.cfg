\* THE HANDOFF-TERMINAL-LEAK (2026-07-26 session mining, 31/68 gate-hitting
\* passes deadlocked to MaxTurns): the runner's per-turn heartbeat
\* (internal/claim Heartbeat -> CommitPaths(planRel()) -> Publish) commits and
\* publishes PLAN.md's on-disk status to `main` UNCONDITIONALLY, independent of
\* the handoff gate and of whether the change doc is anywhere durable. On
\* gate-fail the runner PINS the terminal status in place (swarm.go
\* ~1205/~1401) instead of reverting it, so every later heartbeat republishes
\* it. Durability gate and DoD-check gate both ON (isolating the leak from the
\* older false-DONE defects already covered by TaskStatus_buggy*.cfg): a
\* terminal status still reaches `published` with no change doc ever recorded
\* on `main`. Reproduces the exact shape of beehive:active-state-live-poll and
\* chris-agent:spec-lifecycle-state-machine -- DONE (or NEEDS-REVIEW) on `main`
\* with NO change doc. NoDoclessTerminal violated, with trace.
SPECIFICATION Spec
CONSTANTS
    Limit = 1
    Gated = TRUE
    CheckGated = TRUE
    RevertOverPin = FALSE
    ModelResolve = FALSE
    ResetAttemptsOnResolve = TRUE
INVARIANTS
    TypeOK
    NoDoclessTerminal
